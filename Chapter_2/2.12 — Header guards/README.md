# add.h

# Description
* This add.h header file contains a header guard for the function prototype "int add(int x, int y);".
* The header guard was created by using by the following preprocessor directive:-
* #ifndef
* #define
* #endif

* That's it!, its a really really short project for [question #1].
